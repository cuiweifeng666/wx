<!--pages/fc3dfilter/even/even.wxml-->
<import src="../../common/roundnooption"/>
<view class='even_group'>
    <view class="{{gropu[index] == true?' number_choose_item_red':''}}" wx:for="{{even}}" data-sel="{{index}}" bindtap="changSel">{{item}}</view>
</view>
<template is="footer" data=""/>